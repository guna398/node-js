// loop function if value greater than 10 we use the keyword BREAK to stop

    for (let j = 1; ; j += 2) {
        console.log(j);
            if (j > 10) 
            {
                break;
            }
     }
